import 'package:flutter/cupertino.dart';

class My_admin extends StatelessWidget {
  const My_admin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("MyAdmin");
  }
}
